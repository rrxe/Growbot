.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 18px;
}

.brand {
  display: flex;
  align-items: center;
  gap: 10px;
}

.brand-mark {
  width: 38px;
  height: 38px;
  border-radius: 12px;
  display: grid;
  place-items: center;
  background: #d4ff3d;
  color: #0f0e1b;
  font-weight: 900;
}

.brand strong {
  display: block;
  font-size: 15px;
}

.brand span {
  display: block;
  color: #8b87a8;
  font-size: 10px;
  margin-top: 2px;
}

.icon-button {
  width: 36px;
  height: 36px;
  border: 1px solid #2a2745;
  background: #17152a;
  color: #8b87a8;
  border-radius: 50%;
}

.balance-card {
  position: relative;
  overflow: hidden;
  padding: 22px;
  border-radius: 23px;
  background:
    radial-gradient(
      130% 130% at 100% 0%,
      #24204a,
      #17152a 58%
    );
  border: 1px solid #2a2745;
}

.balance-glow {
  position: absolute;
  width: 170px;
  height: 170px;
  left: -70px;
  bottom: -95px;
  border-radius: 50%;
  background: rgba(212,255,61,.035);
}

.rate-pill {
  position: absolute;
  top: 18px;
  left: 18px;
  background: rgba(212,255,61,.1);
  border: 1px solid rgba(212,255,61,.2);
  color: #d4ff3d;
  padding: 6px 9px;
  border-radius: 20px;
  font-family: ui-monospace, monospace;
  font-size: 10px;
  font-weight: 800;
}

.balance-label {
  color: #8b87a8;
  font-size: 12px;
}

.balance-row {
  display: flex;
  align-items: end;
  gap: 8px;
  margin-top: 7px;
}

.balance-row strong {
  color: #d4ff3d;
  font-size: 39px;
  line-height: 1;
  letter-spacing: -1px;
  font-family: ui-monospace, monospace;
}

.balance-row span {
  color: #8b87a8;
  margin-bottom: 5px;
  font-size: 13px;
}

.balance-sub {
  color: #8b87a8;
  font-size: 11px;
  margin-top: 11px;
}

.quick-actions {
  display: grid;
  grid-template-columns: repeat(3,1fr);
  gap: 10px;
  margin-top: 13px;
}

.quick-action {
  border: 1px solid #2a2745;
  background: #17152a;
  color: #f5f3ff;
  border-radius: 17px;
  padding: 14px 7px;
}

.quick-icon {
  width: 34px;
  height: 34px;
  border-radius: 10px;
  display: grid;
  place-items: center;
  margin: auto auto 8px;
  font-weight: 900;
}

.quick-icon.green {
  color: #d4ff3d;
  background: rgba(212,255,61,.1);
}

.quick-icon.coral {
  color: #ff6b4a;
  background: rgba(255,107,74,.1);
}

.quick-icon.purple {
  color: #c9c6e8;
  background: rgba(139,135,168,.12);
}

.quick-action span {
  font-size: 11px;
  font-weight: 800;
}

.checkin-card {
  margin-top: 18px;
  padding: 16px;
  border-radius: 18px;
  background: #17152a;
  border: 1px solid #2a2745;
}

.checkin-card-top {
  display: flex;
  align-items: center;
  gap: 11px;
}

.checkin-icon {
  width: 43px;
  height: 43px;
  border-radius: 13px;
  display: grid;
  place-items: center;
  background: rgba(90,167,255,.1);
  color: #5aa7ff;
}

.checkin-copy {
  flex: 1;
}

.checkin-copy strong {
  display: block;
  font-size: 13px;
}

.checkin-copy span {
  display: block;
  color: #8b87a8;
  font-size: 10px;
  margin-top: 2px;
}

.checkin-status-badge {
  color: #5aa7ff;
  background: rgba(90,167,255,.1);
  border-radius: 9px;
  padding: 6px 8px;
  font-family: ui-monospace, monospace;
  font-size: 10px;
  font-weight: 900;
}

.checkin-status-badge.done {
  color: #d4ff3d;
  background: rgba(212,255,61,.12);
}

.checkin-meta {
  display: flex;
  justify-content: space-between;
  color: #8b87a8;
  font-size: 9px;
  margin-top: 7px;
}



.home-info-card {
  display: flex;
  gap: 11px;
  margin-top: 12px;
  padding: 14px;
  background: #17152a;
  border: 1px solid #2a2745;
  border-radius: 16px;
}

.info-icon {
  width: 31px;
  height: 31px;
  border-radius: 10px;
  display: grid;
  place-items: center;
  color: #d4ff3d;
  background: rgba(212,255,61,.1);
  font-weight: 900;
}

.home-info-card strong {
  font-size: 11px;
}

.home-info-card p {
  margin: 3px 0 0;
  color: #8b87a8;
  font-size: 9.5px;
  line-height: 1.7;
}

.support-card {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 10px;
  padding: 14px;
  border-radius: 16px;
  border: 1px solid #2a2745;
  background: #201d38;
  color: #f5f3ff;
  text-align: right;
}

.support-card strong,
.support-card span {
  display: block;
}

.support-card strong {
  font-size: 11px;
}

.support-card span {
  margin-top: 3px;
  color: #8b87a8;
  font-size: 9px;
}

.support-card b {
  color: #d4ff3d;
}

.modal-backdrop {
  position: fixed;
  inset: 0;
  z-index: 100;
  display: flex;
  align-items: end;
  justify-content: center;
  background: rgba(4,3,8,.72);
  backdrop-filter: blur(8px);
}

.buy-modal {
  width: 100%;
  max-width: 430px;
  padding: 17px;
  border-radius: 22px 22px 0 0;
  background: #17152a;
  border: 1px solid #2a2745;
}

.modal-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.modal-head span,
.modal-head strong {
  display: block;
}

.modal-head span {
  color: #8b87a8;
  font-size: 10px;
}

.modal-head strong {
  margin-top: 3px;
  font-size: 17px;
}

.modal-head button {
  width: 33px;
  height: 33px;
  border: 1px solid #2a2745;
  border-radius: 50%;
  background: #201d38;
  color: #8b87a8;
  font-size: 19px;
}

.package-grid {
  display: grid;
  grid-template-columns: repeat(2,1fr);
  gap: 8px;
}

.package {
  padding: 13px;
  border-radius: 14px;
  border: 1px solid #2a2745;
  background: #201d38;
  color: #f5f3ff;
  text-align: center;
}

.package.selected {
  border-color: rgba(212,255,61,.35);
  box-shadow: inset 0 0 0 1px rgba(212,255,61,.12);
}

.package strong,
.package span,
.package b {
  display: block;
}

.package strong {
  color: #d4ff3d;
  font-family: ui-monospace, monospace;
  font-size: 19px;
}

.package span {
  color: #8b87a8;
  font-size: 9px;
  margin-top: 2px;
}

.package b {
  color: #f5f3ff;
  margin-top: 8px;
  font-size: 10px;
}

.payment-note {
  color: #8b87a8;
  font-size: 9px;
  line-height: 1.7;
  margin: 12px 2px 0;
}
